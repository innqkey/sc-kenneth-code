server:
  port: 8762

eureka:
  instance:
    hostname: localhost
    instance-id: eureka-server-provided
  client:
    serviceUrl:
      defaultZone: http://${eureka.instance.hostname}:8761/eureka/
      
spring:
  application:
    name: eureka-client
